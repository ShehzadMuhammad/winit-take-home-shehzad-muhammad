services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - MODE=${MODE:-mock}
      - PORT=3000
    volumes:
      - ./fixtures:/app/fixtures:ro
    restart: unless-stopped
